import * as THREE from 'three'; // 1. Scene Setup const scene = new THREE.Scene(); scene.background = new THREE.Color(0x87CEEB); const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000); const renderer = new THREE.WebGLRenderer(); renderer.setSize(window.innerWidth, window.innerHeight); document.body.appendChild(renderer.domElement); // 2. Lights (So we can see the blocks) const light = new THREE.DirectionalLight(0xffffff, 1); light.position.set(5, 10, 7.5).normalize(); scene.add(light); scene.add(new THREE.AmbientLight(0x404040)); // 3. The Floor (Dirt/Grass) const geometry = new THREE.BoxGeometry(1, 1, 1); const material = new THREE.MeshLambertMaterial({ color: 0x228B22 }); // Grass Green for(let x = -5; x < 5; x++) { for(let z = -5; z < 5; z++) { const cube = new THREE.Mesh(geometry, material); cube.position.set(x, -1, z); scene.add(cube); } } camera.position.z = 8; camera.position.y = 5; camera.lookAt(0, 0, 0); // 4. Click to Build window.addEventListener('mousedown', () => { const newBlock = new THREE.Mesh( geometry, new THREE.MeshLambertMaterial({ color: Math.random() * 0xffffff }) ); // Randomly place a block nearby for fun newBlock.position.set( Math.floor(Math.random() * 6) - 3, Math.floor(Math.random() * 3), Math.floor(Math.random() * 6) - 3 ); scene.add(newBlock); }); // 5. Animation Loop function animate() { requestAnimationFrame(animate); scene.rotation.y += 0.005; // Rotate world slowly renderer.render(scene, camera); } animate();
